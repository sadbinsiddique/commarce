spring.application.name=commarce
server.port=8000
coach.name=Sad Bin Siddique

# Log level configuration
#logging.level.org.springframework=DBUG
#logging.level.hibernate=TRACE

# Database configuration
#spring.datasource.url=jdbc:mysql://localhost:3306/commarce_db
#spring.datasource.username=root
#spring.datasource.password=
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Context path of the application
server.servlet.context-path=/v1

# Default HTTPS session timeout (minutes = m, hours = h, days = d)
#server.servlet.session.timeout=5m

# Endpoint to include
#management.endpoints.web.exposure.include=health,info

# Endpoint to exclude
#management.endpoints.web.exposure.exclude=env,beans

# Set base path for actuator endpoints
#management.endpoints.web.base-path=/actuator

# set system username & password for basic authentication
#spring.security.user.name=admin
#spring.security.user.password=changeme
